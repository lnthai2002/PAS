%ul
  -@groups.each do |group|
    %li
      =link_to 'remove', group_path(group), data: {confirm: "Removing #{group.name}?"}, method: :delete
      >
      =link_to group.name, edit_group_path(group)
