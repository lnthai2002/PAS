<%= form_for(@exp_type) do |f| %>
  <% if @exp_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@exp_type.errors.count, "error") %> prohibited this exp_type from being saved:</h2>

      <ul>
      <% @exp_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
