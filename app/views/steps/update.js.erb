//update list of steps
$("#steps").html("<%= escape_javascript(render(:partial => "steps"))%>");
//update step form: turn it back to form to create new step
$("#step_form").html("<%= escape_javascript(render(:partial => "form"))%>");