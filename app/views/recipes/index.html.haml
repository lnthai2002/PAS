%table
  -@recipes.each do |recipe|
    %tr
      %th= link_to recipe.name, recipe
      %td= truncate(recipe.description, :length=>20)
      %td= link_to image_tag('delete.png'), recipe, confirm: 'Are you sure?', method: :delete